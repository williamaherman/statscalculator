
language: python
python:
  - "3.7"
  - "3.8"
  - "3.9"
install:
  - pip install -r requirements.txt
# command to run tests
script:
  - python -m  unittest discover -s  Tests

branches:
  only:
    - master
    - development
    - main